# Archived Phase 5 Gemini Route

Gemini Phase 5B/5C route is abandoned. Preserved artifacts document failed
schema, API-version, model-endpoint, and quota/credential checkpoints. Archive is
not an active implementation, command surface, test suite, configuration, or run
input. No history was rewritten and no failure record was deleted.

`manifest.json` maps every original path to archived path, byte size, and SHA-256.
All 36 archived bytes were verified against commit `9f6c10e`. Historical manifests
inside archive retain original paths by design; archive manifest is current path
authority. Shared provider-neutral prompt, sanitized BM25 candidates, trace
selection, repeatability protocol, and evaluation protocol remain active.
