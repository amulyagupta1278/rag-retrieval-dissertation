git log --all --diff-filter=A --follow --format='%H %aI %s' -- data/raw/snapshot_v1/<file>
shasum -a 256 data/raw/snapshot_v1/* data/metadata/sources.csv data/chunks/chunks_v1.jsonl data/queries/qa_dataset_v1.jsonl data/qrels/qrels.tsv scripts/generate_qa_dataset.py src/ingestion/chunker.py
git show --format=fuller --stat --summary 123932df -- data/raw/snapshot_v1 data/chunks/chunks_v1.jsonl data/metadata/corpus_manifest.json data/metadata/sources.csv data/queries/qa_dataset_v1.jsonl data/qrels/qrels.tsv scripts/generate_qa_dataset.py
git blame --line-porcelain 123932df -- data/queries/qa_dataset_v1.jsonl
git blame --line-porcelain 123932df -- data/qrels/qrels.tsv
jq -r '.category' data/queries/qa_dataset_v1.jsonl | sort | uniq -c
awk 'NR>1 {n++; rel[$4]++; q[$1]++} END {print n; for(k in rel) print k,rel[k]; print length(q)}' data/qrels/qrels.tsv
python3 -c 'import json; from pathlib import Path; from src.ingestion.chunker import Chunker; c=Chunker(chunk_size=300,chunk_overlap=60,min_chunk_length=50,respect_sentence_boundaries=True); committed=[json.loads(x) for x in Path("data/chunks/chunks_v1.jsonl").read_text().splitlines()]; reproduced=[]; [reproduced.extend(c.chunk_document(p.stem,p.read_text(),str(p),"indian_government_welfare_schemes")) for p in sorted(Path("data/raw/snapshot_v1").glob("*.txt"))]; expected={x["chunk_id"]:x["text"] for x in committed}; actual={x.chunk_id:x.text for x in reproduced}; print(sorted((x.doc_id,x.word_count) for x in reproduced)); print(expected.keys()==actual.keys()); print(expected==actual)'
PYTHONHASHSEED=0 python3 -c 'import json; from collections import Counter; from pathlib import Path; from scripts.generate_qa_dataset import QADatasetGenerator; g=QADatasetGenerator(Path("data/chunks/chunks_v1.jsonl"),Path("audits/phase0/midsem_provenance")); g._step1_load_and_analyze(); g._step2_generate_qa_items(); g._step3_build_qrels(); committed=[json.loads(x) for x in Path("data/queries/qa_dataset_v1.jsonl").read_text().splitlines()]; print(len(g.qa_items),Counter(x["category"] for x in g.qa_items),len(g.qrels),g.qa_items==committed)'
